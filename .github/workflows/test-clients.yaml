name: Integration test for beanstalk clients
on: push
jobs:
  build:
    name: Build beanstalkd
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make
      - uses: actions/upload-artifact@v2
        with:
          name: beanstalkd
          path: beanstalkd
  pheanstalk:
    name: Test against Pheanstalk PHP client
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: phpunit
      - uses: actions/download-artifact@v2
        with:
          name: beanstalkd
      - uses: actions/checkout@v2
        with:
          repository: pheanstalk/pheanstalk
          ref: v5
      - name: Start beanstalkd
        run: beanstalkd &
      - name: Wait a few sec so we're sure beanstalkd is up
        run: sleep 2
      - name: Run PHPUnit tests
        run: phpunit
